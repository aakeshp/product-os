# Obsidian
.obsidian/

# Old git history backup (local only)
.git-backup/

# Personal task data (stays local)
Tasks/

# Org-specific work (stays local)
Oak/

# Research (stays local)
Research/

# Agent config with real IDs (stays local)
AGENTS.md

# Local Claude config
.claude/settings.local.json
