# Obsidian
.obsidian/

# Old git history backup (local only)
.git-backup/

# Personal data — now lives in personal-os repo (keep gitignored while local copies exist)
Tasks/
Research/
AI Experiment/

# Org-specific — now lives in oak-knowledge-core repo (keep gitignored while local copies exist)
Knowledge Core/
Discovery Engine/

# Agent config with real IDs (stays local — tracked version is AGENTS.example.md)
AGENTS.md

# Personal skills (stays local — personal-os repo)
.claude/skills/personal-*/

# Local Claude config
.claude/settings.local.json
